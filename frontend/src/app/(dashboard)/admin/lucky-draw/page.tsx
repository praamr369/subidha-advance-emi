import PortalPage from "@/components/ui/portal-page";

export default function AdminLuckyDrawPage() {
  return <PortalPage title="Lucky Draw Execution" subtitle="Commit-reveal winner execution with atomic waiver updates and immutable draw history." />;
}
