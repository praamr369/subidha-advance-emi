import PortalPage from "@/components/ui/portal-page";

export default function PartnerSubscriptionsPage() {
  return <PortalPage title="Partner Subscriptions" subtitle="Create subscriptions only on available lucky IDs and monitor status read-only." />;
}
