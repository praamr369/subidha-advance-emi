import PublicNav from "@/components/ui/public-nav";
import PortalPage from "@/components/ui/portal-page";

export default function VisionTrustPage() {
  return (
    <PortalPage title="Vision & Trust" subtitle="Transparency, compliance and customer confidence at every financial step.">
      <PublicNav />
      <p>
        The platform is built for role-based accountability, immutable event history, and expandable finance modules such as
        leasing and marketplace operations.
      </p>
    </PortalPage>
  );
}
