import PublicNav from "@/components/ui/public-nav";
import PortalPage from "@/components/ui/portal-page";

export default function AboutPage() {
  return (
    <PortalPage title="About Subidha Furniture" subtitle="Retail-focused EMI and lucky draw systems for trusted furniture ownership.">
      <PublicNav />
      <p>
        Subidha Furniture, Asansol, runs the Lucky Plan as a finance-first platform with transparent subscription tracking,
        winner history visibility, and auditable accounting for every EMI lifecycle event.
      </p>
    </PortalPage>
  );
}
