/**
 * Discoverability alias for loading surfaces (same as LoadingBlock).
 * Prefer `LoadingBlock` in code that already uses it; use this name in new page shells.
 */
export { default } from "./LoadingBlock";
